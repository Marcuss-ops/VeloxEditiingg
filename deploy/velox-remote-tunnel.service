[Unit]
Description=Persistent Velox SSH forwards (master, creator, gRPC, OpenBao)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
EnvironmentFile=%h/.config/velox/remote-tunnel.env
Environment=VELOX_TUNNEL_OPENBAO_PORT=18200
Environment=VELOX_REMOTE_OPENBAO_PORT=8200
ExecStart=/usr/bin/ssh -N -T \
  -p ${VELOX_REMOTE_SSH_PORT} \
  -o BatchMode=yes \
  -o ControlMaster=no \
  -o ControlPath=none \
  -o ExitOnForwardFailure=yes \
  -o ServerAliveInterval=30 \
  -o ServerAliveCountMax=3 \
  -o TCPKeepAlive=yes \
  -o StrictHostKeyChecking=yes \
  -L 127.0.0.1:${VELOX_TUNNEL_MASTER_PORT}:127.0.0.1:${VELOX_REMOTE_MASTER_PORT} \
  -L 127.0.0.1:${VELOX_TUNNEL_CREATOR_PORT}:127.0.0.1:${VELOX_REMOTE_CREATOR_PORT} \
  -L 127.0.0.1:${VELOX_TUNNEL_GRPC_PORT}:127.0.0.1:${VELOX_REMOTE_GRPC_PORT} \
  -L 127.0.0.1:${VELOX_TUNNEL_OPENBAO_PORT}:127.0.0.1:${VELOX_REMOTE_OPENBAO_PORT} \
  ${VELOX_REMOTE_SSH_USER}@${VELOX_REMOTE_SSH_HOST}
ExecStartPost=/usr/local/bin/check-local-velox-tunnel ${VELOX_TUNNEL_MASTER_PORT} ${VELOX_TUNNEL_CREATOR_PORT} ${VELOX_TUNNEL_GRPC_PORT} ${VELOX_TUNNEL_OPENBAO_PORT}
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
