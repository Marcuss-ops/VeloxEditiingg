fix(store): Batch 3.2 amendment \u2014 remove stray closing brace in store_jobs_history.go

The Batch 3.2 deletion commit (e32ca965) on main shipped with a stray
closing brace in store_jobs_history.go \u2014 the second str_replace for
deleting GetJobLogs set new = "\}\n" instead of new = "", which added
an extra closing brace after replaceJobHistoryTx. Result: go build/vet
failed with "syntax error: non-declaration statement outside function
body" at line 25.

This amendment:
- Removes the stray `}` after replaceJobHistoryTx's closing brace
  (the file now ends correctly with the replaceJobHistoryTx close)

NOTE: The `time` import Batch 3.2 reviewer flagged as unused was
ALREADY removed in Batch 3.1 (commit 0c6da327) when AddJobHistory and
AddJobLog (the last `time.Now()` consumers in this file) were deleted.
No import fix needed.

Verified: go build, go vet, race-test on jobs/queue/store, full
short test. replaceJobHistoryTx still functional in ClaimNextPendingJob.
